namespace tictactoe
{
    /// <summary>
    /// X should be the first Player
    /// O should be the second Player
    /// _ is "empty", which should fill the board at first
    /// </summary>
    public enum PlayerMark { X, O, _ };
}